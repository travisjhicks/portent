# Portent

Portent is an open specification for portable knowledge base systems.

It models knowledge as typed objects and explicit relationships, then lets tools render the views they need.

## Website

```bash
pnpm install
pnpm docs:dev
```

The VitePress site lives in `site/`.
